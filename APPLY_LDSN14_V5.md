# LDSN1-4 V5

Giải nén và chép toàn bộ nội dung vào thư mục gốc `tieng-trung-web`, chọn ghi đè file trùng tên.

V5 giữ nguyên các khóa lưu hiện tại:

- `tiengTrung.ldsn14.settings.v1`
- `tiengTrung.ldsn14.progress.v1`

Kiểm tra local:

```powershell
python -m http.server 8000
```

Mở `http://localhost:8000/modules/ldsn14/?lesson=1`.
