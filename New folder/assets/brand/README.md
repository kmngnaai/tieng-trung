# Mascot asset

File used by the interface: `mascot.png`.

To change the mascot later, replace this file with another PNG using the same filename. The Pinyin interface references this asset by path and does not embed the image in JavaScript.
